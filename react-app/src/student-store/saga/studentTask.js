import { take } from 'redux-saga/effects';
import { actionTypes } from '../action/student/searchCondition';



export default function* studentTask() {
    // let action = yield take(actionTypes.asyncIncrease);
    // console.log('increase 异步改变', action);
}